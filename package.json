{
  "name": "hireit-server",
  "version": "1.0.0",
  "description": "HireIt Stripe backend server",
  "main": "server.js",
  "scripts": {
    "start": "node -e \"console.log('KEY_SUFFIX:', process.env.STRIPE_SECRET_KEY?.slice(-4))\" && node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.1",
    "express": "^4.18.2",
    "resend": "^3.5.0",
    "stripe": "^14.17.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}